/* 
----------------------------------------
# npm install 
By default, npm install will install all modules listed as dependencies in package.json in the local node_modules folder.

----------------------------------------
# npm install -g eslint
-g or --global installs the current package context In global mode, as a global package.

----------------------------------------
# npm install -D webpack-dev-server
-D or --save-dev installs the current package context as `devDependencies`.

----------------------------------------
# npm install --production
with --production flag npm doesn't install modules listed in devDependencies

----------------------------------------
# npm install why-did-you-update@v0.1.0 
Install the version of the package that is referenced by the specified tag

----------------------------------------
*/
